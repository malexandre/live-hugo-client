{
  "name": "live-hugo-client",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "format-date": "^1.0.0",
    "malexandre-react-scripts": "1.1.0",
    "react": "^16.0.0",
    "react-dom": "^16.0.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "proxy": {
    "/api": {
      "target": "http://localhost:4000"
    }
  }
}
